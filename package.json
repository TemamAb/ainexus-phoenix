{
  "name": "quantumnex-dashboard",
  "version": "1.0.0",
  "description": "QuantumNex AI Trading Engine Dashboard",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "echo 'No build needed - using Express fallback'",
    "render-build": "npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
