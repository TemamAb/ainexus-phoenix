import React, { useEffect, useState } from 'react'\nimport useBlockchainStream from '../hooks/useBlockchainStream'\n\nexport default function LiveStream() {\n  const { events, connect } = useBlockchainStream()\n\n  useEffect(() => {\n    connect()\n  }, [connect])\n\n  return (\n    <div className="card">\n      <h3>Live Blockchain Events</h3>\n      <div style={{ maxHeight: 200, overflow: 'auto' }}>\n        {events.length === 0 ? <div>No events</div> : events.map((e, i) => <div key={i}>{JSON.stringify(e)}</div>)}\n      </div>\n    </div>\n  )\n}