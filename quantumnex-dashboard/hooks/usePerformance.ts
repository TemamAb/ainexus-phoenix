import { useEffect, useState } from 'react'\n\nexport default function usePerformance() {\n  const [metrics, setMetrics] = useState({ profitHour: 0, tradesDay: 0, latency: 0 })\n\n  useEffect(() => {\n    const t = setInterval(() => {\n      // placeholder: in real app fetch streaming metrics\n      setMetrics({ profitHour: Math.random() * 10, tradesDay: Math.floor(Math.random() * 100), latency: Math.random() * 200 })\n    }, 5000)\n    return () => clearInterval(t)\n  }, [])\n\n  return { metrics }\n}