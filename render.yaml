services:
  - type: web
    name: ainexus-platform
    env: node
    plan: free
    region: oregon
    buildCommand: |
      pip install -r requirements.txt
      npm install
    startCommand: npm start
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
      - key: NODE_ENV
        value: production
