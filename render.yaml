services:
  - type: web
    name: ainexus-platform
    env: docker
    plan: free
    region: oregon
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
