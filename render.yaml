services:
  - type: web
    name: ainexus-phoenix
    docker:
      dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: ENVIRONMENT  
        value: production
    autoDeploy: true
