services:
  - type: web
    name: ainexus-phoenix
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0  # Explicitly set to 3.11
      - key: ENVIRONMENT
        value: production
      - key: PORT
        value: 10000
    autoDeploy: true

  - type: worker
    name: ainexus-trading-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_engine.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: ENVIRONMENT
        value: production
