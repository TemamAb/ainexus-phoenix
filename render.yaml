services:
  - type: web
    name: ainexus-phoenix-live
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd core && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
