services:
  adaptive-profit-engine:
    type: web
    name: ainexus-adaptive-profit
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEPLOYMENT_ENV
        value: production
    plan: free
    autoDeploy: true
