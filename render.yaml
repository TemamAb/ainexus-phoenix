services:
  - type: web
    name: ainexus-platform
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python core/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
