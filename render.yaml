services:
  ainexus-profit-engine:
    type: worker
    name: ainexus-profit-engine
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEPLOYMENT_ENV
        value: production
    plan: free
    autoDeploy: true
