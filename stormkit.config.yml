name: ainexus
type: python

build:
  command: |
    pip install -r requirements.txt

run:
  command: python core/app.py

routes:
  - src: /*
    dest: /

environment:
  - name: PORT
    value: "8000"
  - name: PYTHON_VERSION  
    value: "3.11"
