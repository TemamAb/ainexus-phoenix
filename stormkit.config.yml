name: ainexus
type: node

build:
  command: echo "No build needed for Python"

deploy:
  command: |
    pip install -r requirements.txt
    
run:
  command: python core/app.py
  
routes:
  - src: /*
    dest: /
    
environment:
  - name: PORT
    value: "8000"
